// Stub: no-op seed function — translations are pre-seeded server-side
export async function seedHomeTrainingTranslations() {
  // No-op in the frontend bundle; actual seeding done via backend function.
  return true;
}
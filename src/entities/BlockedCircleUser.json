{
  "name": "BlockedCircleUser",
  "type": "object",
  "properties": {
    "circle_id": {
      "type": "string",
      "description": "Reference to PrayerCircle"
    },
    "blocked_by": {
      "type": "string",
      "description": "Email of user doing blocking"
    },
    "blocked_user": {
      "type": "string",
      "description": "Email of blocked user"
    },
    "blocked_user_name": {
      "type": "string",
      "description": "Display name of blocked user"
    }
  },
  "required": ["circle_id", "blocked_by", "blocked_user", "blocked_user_name"]
}